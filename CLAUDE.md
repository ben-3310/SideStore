# Локальна інваріанта: `Dependencies`

`Dependencies` — захищене абсолютне символьне посилання на
`/Users/ben/Repo/SideStore_Dependencies`.

Не видаляй, не переміщай, не замінюй і не відновлюй цю директорію через Git,
submodule, build-скрипти чи cleanup. Не змінюй її ціль. Перед діями, що можуть
її зачепити, перевір лише `ls -ld Dependencies` та `readlink Dependencies`.
Змінювати посилання або ціль можна лише після явного дозволу користувача.
@ASC.md
