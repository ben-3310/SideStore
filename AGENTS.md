# Захист `Dependencies`

`Dependencies` є локальним абсолютним символьним посиланням на
`/Users/ben/Repo/SideStore_Dependencies`.

- Не видаляти, не переміщати, не замінювати та не перестворювати це посилання.
- Не запускати в ньому `git submodule`, `git clean`, генератори залежностей або
  інші команди, що можуть змінити посилання чи його ціль.
- Перед будь-якою операцією, яка зачіпає `Dependencies`, дозволені лише
  перевірки `ls -ld Dependencies` і `readlink Dependencies`.
- Будь-яка зміна посилання або його цілі потребує явного дозволу користувача.
